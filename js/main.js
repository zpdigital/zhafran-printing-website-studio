const productGrid=document.getElementById("productGrid"),galleryGrid=document.getElementById("galleryGrid"),menuBtn=document.getElementById("menuBtn"),navLinks=document.getElementById("navLinks");
function waLink(product,price){return `https://wa.me/${phoneNumber}?text=${encodeURIComponent(`Halo ZHAFRAN, saya ingin pesan ${product}. Harga: ${price}`)}`}
function renderProducts(){if(!productGrid)return;productGrid.innerHTML=products.map(item=>`<article class="product-card reveal"><div class="product-media"><img src="${item.image}" alt="${item.title}" loading="lazy"><span class="badge">${item.badge}</span></div><div class="product-body"><h3>${item.title}</h3><p>${item.desc}</p><strong>${item.price}</strong><a class="btn primary full" href="${waLink(item.title,item.price)}" target="_blank">Pesan Sekarang</a></div></article>`).join("")}
function renderGallery(){if(!galleryGrid)return;galleryGrid.innerHTML=galleryItems.map(item=>item.type==="video"?`<article class="gallery-card reveal"><video controls playsinline preload="metadata"><source src="${item.src}" type="video/mp4"></video><h3>${item.title}</h3></article>`:`<article class="gallery-card reveal"><img src="${item.src}" alt="${item.title}" loading="lazy"><h3>${item.title}</h3></article>`).join("")}
function revealOnScroll(){document.querySelectorAll(".reveal").forEach(el=>{if(el.getBoundingClientRect().top<window.innerHeight-80)el.classList.add("show")})}
function parallaxMove(){const s=window.scrollY;document.querySelectorAll("[data-parallax]").forEach(el=>{el.style.transform=`translate3d(0,${s*Number(el.dataset.parallax||.1)}px,0)`})}
menuBtn?.addEventListener("click",()=>navLinks.classList.toggle("active"));window.addEventListener("scroll",()=>{revealOnScroll();parallaxMove()});renderProducts();renderGallery();revealOnScroll();